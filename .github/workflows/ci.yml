name: CI

on:
  push:
    branches: [main, master]
  pull_request:

# Normal Push CI (Tier 1): minutes, not hours.
# Full 25/50/100 certification lives in certification.yml (manual / scheduled).

jobs:
  quality:
    name: Quality
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 9.15.9
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: pnpm
      - name: Install
        run: pnpm install --frozen-lockfile
      - name: Build
        run: pnpm build
      - name: Typecheck
        run: pnpm typecheck
      - name: Lint
        run: pnpm lint
      - name: Format
        run: pnpm format:check
      - name: Fast tests
        run: pnpm test:fast
      - name: Validate content (TypeScript)
        run: pnpm validate:content
      - name: Validate content (Python)
        run: python3 scripts/validate_content.py
      - name: Short world smoke (2y)
        run: pnpm test:smoke

  integration:
    name: Integration
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 9.15.9
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: pnpm
      - name: Install
        run: pnpm install --frozen-lockfile
      - name: Build
        run: pnpm build
      - name: Integration tests
        run: pnpm test:integration
